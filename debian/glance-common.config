#!/bin/sh

set -e

. /usr/share/debconf/confmodule

db_input high glance/paste-flavor || true
db_go || true
db_get glance/paste-flavor

case "$RET" in
    keystone*)
        db_input high glance/auth-url || true
        db_input high glance/auth-token || true
        db_go || true
        ;;
esac
