#!/bin/sh

set -e

. /usr/share/debconf/confmodule

db_get glance/paste-flavor

case "$RET" in
    keystone*)
        db_input high glance-api/auth-url || true
        db_input high glance-api/auth-token || true
        db_go || true
        ;;
esac
